import FirebaseProductsContainer from "../../containers/products/FirebaseProductsContainer.js"

export default class FirebaseProductsDao extends FirebaseProductsContainer {
    constructor(uri){
        super(uri)
    }
}